{
  "version": "1.19t",
  "extra": "",
  "fixes": [],
  "features": [
    "Overflowing web extensions will now appear below the urlbar instead of being added back into the site control panel. This only happens in single-sidebar mode.",
    "Enso will now follow the XDG Base Directory Specification for storing its configuration and data files. This means that on Linux, configuration files will be stored in `~/.config/enso`. Note that compatibility with the old configuration directory (`~/.enso`) will be enabled if the directory exists.",
    "Dragging a tab to the center of the window to create a split will now give the option to split on top or below."
  ]
}
